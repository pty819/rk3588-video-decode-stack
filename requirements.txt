# Pinned versions — local dev and CI must use the same versions
# so the docs don't break because of a transitive dep bump.
Sphinx==9.0.4
myst-parser==5.1.0
furo==2025.12.19
sphinx-copybutton==0.5.2
sphinx-togglebutton==0.4.5
sphinx-design==0.7.0
